/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tells Next.js 16 we acknowledge Turbopack is handling the build
  turbopack: {}, 
  
  // You can put other standard Next.js options here if you have them (e.g., images, redirects)
};

export default nextConfig;